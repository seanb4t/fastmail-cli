# Cocogitto conventional commit configuration
# Validation only — release-please handles versioning

[settings]
ignore_merge_commits = true

[commit_types]
feat = {}
fix = {}
docs = {}
style = {}
refactor = {}
perf = {}
test = {}
build = {}
ci = {}
chore = {}
revert = {}
